import { SectionCard } from '../SectionCard'

export default function Expenses() {
    return (
        <SectionCard 
            icon='ph ph-basket'
            text='todas as'
            strongText='despesas'
        >            
        </SectionCard>
    )
}